{
  "name": "minesweeper",
  "version": "1.0.0",
  "description": "",
  "main": "scripts/main.js",
  "scripts": {
    "test:e2e": "cucumber-js --require cucumber.conf.js --require tests/steps/**/*.steps.js --format @cucumber/pretty-formatter --format html:tests/reports/cucumber-report.html --tags  \"not @Manual\" \"not @done\"  --parallel 4"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^8.5.2",
    "@cucumber/pretty-formatter": "^1.0.0",
    "@playwright/test": "^1.25.1",
    "playwright": "^1.25.1"
  }
}
